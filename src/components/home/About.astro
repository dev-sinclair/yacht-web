---
import { Image } from "astro:assets";
import Container from "../ui/Container.astro";
import mapOutline from "../../assets/mapoutline.webp";

const pillars = [
  { label: "Private", text: "One broker, one client. Direct and personal." },
  { label: "Global", text: "Curated yachts across the world's finest waters." },
  { label: "Bespoke", text: "Every itinerary shaped around you." },
  { label: "Discreet", text: "Confidentiality at every step of the journey." },
];
---

<section id="about" class="relative overflow-hidden bg-white pt-20 pb-32 lg:py-44">
  <!-- Map background -->
  <div class="absolute inset-0 pointer-events-none">
    <Image
      src={mapOutline}
      alt=""
      class="absolute inset-0 h-full w-full object-cover opacity-70"
      widths={[1280, 1920]}
      sizes="100vw"
      loading="lazy"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-white/60 via-transparent to-white/60"></div>
  </div>

  <Container>
    <div class="relative">
      <div>
        <div data-reveal>
          <p class="text-[11px] font-light tracking-[0.3em] uppercase text-deep/70">About Us</p>
          <!-- <h2
            class="mt-4 text-3xl font-light tracking-[0.15em] uppercase text-charcoal sm:text-4xl lg:text-5xl"
            style="font-family: 'Raleway', sans-serif; font-weight: 300;"
          >
            A Private Passage<br />
            Across Every Horizon
          </h2> -->
          <div class="mt-6 h-px w-10 bg-deep/40"></div>
          <p class="mt-8 max-w-xl text-base leading-[1.8] text-taupe">
            Sinclair Yachting is a boutique brokerage connecting a select clientele with exceptional yachts across the world's finest cruising grounds. Quiet, personal, and deliberate at every step.
          </p>
        </div>

        <!-- Pillars -->
        <div class="mt-12 grid grid-cols-1 gap-y-8 sm:grid-cols-2 sm:gap-x-8 lg:grid-cols-4 lg:gap-x-10" data-reveal>
          {pillars.map((p) => (
            <div>
              <p
                class="text-xl font-light tracking-[0.25em] uppercase text-charcoal sm:text-2xl"
                style="font-family: 'Raleway', sans-serif; font-weight: 300;"
              >
                {p.label}
              </p>
              <p class="mt-2 text-sm leading-[1.7] text-taupe">
                {p.text}
              </p>
            </div>
          ))}
        </div>

        <!-- Primary CTA: yacht charter is the main purpose of the site, so
             this is the hero action. Centered, filled, oversized vs. other
             buttons in the page rhythm. -->
        <div class="mt-16 flex flex-col items-center lg:mt-20" data-reveal>
          <p class="text-[11px] font-light tracking-[0.3em] uppercase text-deep mb-6">
            Start Your Journey
          </p>
          <a
            href="/yachts"
            class="group relative inline-flex items-center gap-4 overflow-hidden bg-deep px-10 py-5 text-[12px] font-medium tracking-[0.3em] uppercase text-white shadow-md transition-all duration-300 hover:bg-charcoal hover:shadow-lg sm:px-14 sm:text-[13px]"
          >
            <span class="relative z-10">View Available Yachts</span>
            <svg
              class="relative z-10 h-4 w-4 transition-transform duration-300 group-hover:translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
          <p class="mt-4 text-[10px] font-light tracking-[0.2em] uppercase text-taupe-light">
            Browse over 750 charter yachts worldwide
          </p>
        </div>
      </div>
    </div>
  </Container>
</section>
